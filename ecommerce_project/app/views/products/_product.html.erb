<h3 class="title is-size-4"><%= link_to product.product_name, product %></h3>
    <h4 h3 class="title is-size-5">Price: $<%= product.price %></h4>
        <% if product.image? %>
            <p class= "center"><%= image_tag product.image.thumbnail.url %></p>
        <%end%>