<h3 class="title is-4"><%= link_to product.product_name, product %></h3>
    <h4 h3 class="title is-5">Price: $<%= product.price %></h4>
        <% if product.image? %>
            <p><%= image_tag product.image.thumbnail.url %></p>
        <%end%>